peremeter
	
	一般的就不写了

	特殊参数
		&引用
			能对实际的值产生更改

Vector(Vector &&rhs)

	这是一种什么写法

	两个&& 表达了什么意思

	现在在按照教程自己编写vector容器，Vector是类名

左值和右值
	左值编译器使用的是他的地址
	右值使用的是他的内容

	右值只能放在右面

	引用参数
		相当于传递的是地址，让函数知道相应的位置

	const int &x
	参数x不能被修改
		传递值的速度更快

	返回引用的目的是为了继续对函数进行操作
