装填因子
	n/m
		n表示存在的数量，m表示表的大小
基本知识复习
	explicit
		防止误会，不会使用自动转换进行操作
	size_t
		无符号整数
	enum
		特殊情况
		用于case判断，表示一种特殊的class类，switch语句中使用
	std::move()
		 用于函数构造，移动构造，但是使用可以更快的进行操作
		 std::move是将对象的状态或者所有权从一个对象转移到另一个对象，只是转移，没有内存的搬迁或者内存拷贝。
	左值右值
		（（（（（））））））
	